import { AgeInMonthsPipe } from './age-in-months.pipe';

describe('AgeInMonthsPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeInMonthsPipe();
    expect(pipe).toBeTruthy();
  });
});
